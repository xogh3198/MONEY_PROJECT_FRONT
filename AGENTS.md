# Repository agent instructions

Before every task, read and obey `.agents/rules/parallel-work.md`. These instructions are mandatory
for Codex, Antigravity, and any other coding agent working in this repository.

This repository is authorized only as `xogh3198/MONEY_PROJECT_FRONT`. The only related GitHub
repository that may also be accessed is `xogh3198/MONEY_PROJECT`. Do not access other GitHub
repositories or use broad account-level repository search/listing.

Use the personal Notion workspace and unified dashboard defined in `.agents/rules/parallel-work.md`.
Check Notion and GitHub before editing, claim paths, use a separate worktree and agent-prefixed branch,
and leave verified commit and deployment evidence when finished.

The full human workflow is in `docs/AI_COLLABORATION.md`.
